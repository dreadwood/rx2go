mixin btn-table({text, icon, isError, isDisabled})
  button.btn-table(class=(isError && "btn-table--error") disabled=isDisabled)
    if icon
      +svg({name: icon, width: 17, height: 17, className: "btn-table__icon"})
    =text

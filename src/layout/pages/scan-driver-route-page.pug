extends ../elements/layout
include ../templates/btn-panel

block data
  - title = "Scan: driver out, route select, without vendor"

block content
  +btn-panel({})

  br

  .form-field-wrp
    label.form-field-wrp__label Select a Route:

    .form-select-input.js-form-select-input
      .form-select-input__field
        input.form-select-input__input(type="text" placeholder="Select a Route...")
        button.form-select-input__toggle(onclick="window.utils.toggleCssClassNodes(this, 'js-form-select-input', 'open')")
          +svg({name: "icon-arrow-dropdown", width: 20, height: 16})
      .form-select-input__dropdown
        each it in (new Array(7).fill(''))
          button.form-select-input__item #[b NH Q - 1]  |  #[13 Orders]  |  21 September 01:56 pm
